{% from "locale-fix.njk" import localeUrlFixDaisy with context %}

<div class="border-b border-base-300 dark:border-gray-700 text-center text-xs px-2 md:px-0">
  <div class="max-w-6xl mx-auto text-base-content flex justify-end md:justify-between">
      
      
      <div class="hidden md:flex items-center text-xs text-neutral-400 pl-5">
           {{ languages[page.lang].texts.protectingPlayersSince }}
      </div>

      <ul class="flex justify-end px-2 md:px-0 items-center">
        {# Add casino button #}
        <li class="flex flex-row items-center">
          <a target="_blank" 
                class="hover:underline flex flex-row items-center"
                rel="nofollow noopener noreferrer" 
                href="{{ languages[page.lang].top.url }}" 
                onclick="gtag('event', 'click', { 
                    event_category: 'link', 
                    event_label: 'top-link', 
                    value: 1 
                });">
                <i data-lucide="circle-plus" strokeWidth={1} class="h-3 text-secondary"></i>
                {{ languages[page.lang].top.text }}
          </a>
        </li>

        <li class="hidden md:inline-flex text-neutral-content mx-3">/</li>

        <li class="hidden md:flex flex-row items-center">
          <a class="hover:underline flex flex-row items-center"
                href="{{ '/transparencia/' | locale_url }}">
                {{ languages[page.lang].texts.transparency }}
          </a>
        </li>  


        <li class="text-neutral-content mx-3">/</li>

        {# About us  #}
        <li class="flex flex-row items-center">
          <a class="hover:underline flex flex-row items-center"
                href="{{ '/nosotros/' | locale_url }}">
                {{ languages[page.lang].texts.aboutUs }}
          </a>
        </li>  

        <li class="text-neutral-content mx-3">/</li>


        {# Theme Toggle #}
        <li class="flex flex-row items-center">
          <button onclick="const html = document.documentElement; const currentTheme = html.getAttribute('data-theme'); const newTheme = currentTheme === 'dark' ? 'cmyk' : 'dark'; html.setAttribute('data-theme', newTheme); localStorage.setItem('bz_theme', newTheme); this.textContent = newTheme === 'dark' ? '🌙' : '☀️';" 
                  class="btn btn-ghost btn-circle btn-xs hover:bg-base-200" 
                  title="Toggle theme">
            ☀️
          </button>
        </li>        


        <li class="text-neutral-content mx-3">/</li>

        {# Global Search Modal Trigger #}
        <li class="flex flex-row items-center">
          <button id="global-search-modal-trigger" class="btn btn-ghost btn-circle btn-xs">
            <i data-lucide="search" class="h-4"></i>
          </button>
        </li>

        <li class="text-neutral-content mx-3">/</li>

        {# Language Modal Trigger #}
        <button id="language-modal-trigger" class="btn btn-ghost btn-circle">
            <div class="indicator">
                <i data-lucide="languages" class="h-4 md:h-4"></i>
                <span class="badge badge-xs p-0 indicator-item">
                    <img src="{{ languages[page.lang].menu.flagSvg }}" class="h-4" alt="flag" />
                </span>
            </div>
        </button>

        {% include "templates/languages-modal.template.njk" %}        

      </ul>

    </div>
</div>
